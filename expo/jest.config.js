/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  rootDir: '.',
  roots: ['<rootDir>/utils', '<rootDir>/constants'],
  testMatch: ['**/__tests__/**/*.test.ts', '**/*.test.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/$1',
  },
  transform: {
    '^.+\\.tsx?$': [
      'ts-jest',
      {
        tsconfig: '<rootDir>/tsconfig.test.json',
      },
    ],
  },
  // React Native / Expo modules that the resolver shouldn't touch in pure-utils tests.
  modulePathIgnorePatterns: ['<rootDir>/.expo', '<rootDir>/node_modules/expo'],
  clearMocks: true,
};
